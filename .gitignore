# ─────────────────────────────────────────────────────────────────────────────
# .gitignore for the Crenox project
# ─────────────────────────────────────────────────────────────────────────────

# Build outputs
dist/
/crenox
*.exe

# Test coverage artifacts
coverage.out
coverage.html

# Go tool cache
vendor/

# Editor & OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/
.cursor/
.windsurf/
.notes/
.clinerules
.cursorrules
.windsurfrules
*.swp
*.swo
*~

# Sensitive config (never commit real credentials)
.crenox.yaml
!.crenox.yaml.example

# Agent instruction cache & lockfiles
.agents/
skills-lock.json

# Cloned local repositories
nexusfi-web/

# ─────────────────────────────────────────────────────────────────────────────
# Dashboard (private feature — not published to the public repo)
# Build locally with: go build -tags dashboard ./cmd/crenox
# ─────────────────────────────────────────────────────────────────────────────
internal/web/
cmd/crenox/commands/dashboard.go
